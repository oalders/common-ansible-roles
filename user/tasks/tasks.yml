---
- name: Maybe create group
  group:
    name: '{{app_user}}'
    state: present
- name: Maybe create user {{app_user}} with home dir {{user_home}}
  user:
    createhome: yes
    group: '{{app_user}}'
    home: '{{user_home}}'
    name: '{{app_user}}'
    shell: /bin/bash
