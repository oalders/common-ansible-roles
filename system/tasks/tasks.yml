---
- apt:
  name: "{{ packages }}"
  vars:
    packages:
      - curl
      - git
      - golang
      - lynx
      - ncdu
      - nmap
      - npm
      - ntp
      - perltidy
      - ruby
      - tig
      - tree
      - unzip
- include_role:
    name: oefenweb.updatedb
