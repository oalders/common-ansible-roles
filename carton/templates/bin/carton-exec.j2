#!/bin/bash

{% for key, value in app.carton.env.items() %}
export {{key}}={{value}}
{% endfor %}

cd {{app.checkout_root}}
/usr/local/bin/carton exec -- perl -Ilib {{ app.carton.includes | default('') }} $@
