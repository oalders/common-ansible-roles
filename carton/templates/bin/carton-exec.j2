#!/bin/bash

{% for key, value in app.value.carton.env.items() %}
export {{key}}={{value}}
{% endfor %}

cd {{app.value.checkout_root}}
/usr/local/bin/carton exec -- perl -Ilib {{ app.value.carton.includes | default('') }} $@
