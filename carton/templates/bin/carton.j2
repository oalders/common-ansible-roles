#!/bin/bash

export PERL_CARTON_PATH={{app.checkout_root}}/local

{% for key, value in app.carton.env.items() %}
export {{key}}={{value}}
{% endfor %}

cd {{app.checkout_root}}
/usr/local/bin/carton $@
