#!/bin/bash

export PERL_CARTON_PATH={{app.value.checkout_root}}/local

{% for key, value in app.value.carton.env.items() %}
export {{key}}={{value}}
{% endfor %}

cd {{app.value.checkout_root}}
/usr/local/bin/carton $@
